import 'package:flutter/material.dart';

class UserFormScrn extends StatelessWidget {
  const UserFormScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0Xff188F79),
    );
  }
}
